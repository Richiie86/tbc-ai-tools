# CRA / react-scripts expects a flat node_modules. pnpm's default symlinked
# layout breaks it, so force a hoisted layout.
node-linker=hoisted
shamefully-hoist=true
# Don't abort when a package declares a different packageManager.
package-manager-strict=false
